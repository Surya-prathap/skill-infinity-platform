/**
 * Communication feature data.
 *
 * No dummy/seed data — all conversations, messages, presence, announcements
 * and notifications are loaded from the communication-service via the API
 * and real-time socket. This module only re-exports types used by the domain.
 */
export {};
